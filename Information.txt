
npm i express dotenv cors axios cloudinary multer

📦 1. express-A minimal and fast Node.js web framework for building APIs and web servers.
Provides routing, middleware, and HTTP utility methods to handle client requests and responses.

📦 2. dotenv-Loads environment variables from a .env file into process.env.
Keeps sensitive information (API keys, DB credentials) secure and out of code.

📦 3. cors-Middleware for enabling Cross-Origin Resource Sharing (CORS) in Express apps.
Allows frontend (React) to make requests to backend (Express) hosted on a different domain/port.

📦 4. axios-A promise-based HTTP client for making API requests in Node.js and browsers.
Easier syntax than fetch and supports interceptors, error handling, and automatic JSON parsing.

📦 5. cloudinary-A cloud service for image and video storage, optimization, and delivery.
Provides an API to easily upload, transform, and serve media files in web apps.

📦 6. multer-A middleware for handling multipart/form-data (mainly file uploads) in Express.
Stores uploaded files locally or passes them to cloud services (e.g., Cloudinary).

npm install -D vite @vitejs/plugin-react @tailwindcss/vite
📦-D (or --save-dev) installs the packages as devDependencies, meaning they’re only needed during development, not in production.
📦The command installs Vite, its React plugin, and the Tailwind + Vite plugin so your React + Tailwind project can run with Vite’s fast bundler. ✅

✅ npm i react-router-dom is the command to install React Router DOM, a library used for routing in React applications.
✅ It helps in navigating between different pages (components) of a React app without reloading the browser.


https://prebuiltui.com/
Explore the Ultimate Collection of UI Components

amilegend19999_db_user
0hnhUA4k1XRi9qoP

mongodb
https://cloud.mongodb.com/v2/68e38baef7fe47272d764eaa#/addData/Cluster0/create
https://cloud.mongodb.com/v2/68e38baef7fe47272d764eaa#/metrics/replicaSet/68e38c18185f470e9aced869/explorer/compilex/users/find

Cloudinary
https://console.cloudinary.com/app/c-e64d39508c47771fcc1f830a1e7630/settings/api-keys